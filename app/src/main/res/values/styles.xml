<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Material.Light">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorPrimary">@color/UW_purple</item>
        <item name="android:editTextBackground">@color/UW_gold</item>
    </style>

    <style name="TextStyle" >
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textColor">?android:textColorPrimary</item>
    </style>

    <style name="TextStyle.LabelStyle">
        <item name="android:textStyle">bold</item>
    </style>

    <style name="TextStyle.LabelStyle.updatedSyle">
        <item name="android:background">?android:editTextBackground</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="ButtonStyle" parent="@android:style/MediaButton">
        <item name="android:textSize">22sp</item>
    </style>
</resources>
